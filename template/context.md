# Context

> Injected into every session in this vault. Keep it under 40 lines — the whole file
> enters the context window every time. It is a snapshot of *now*, not a history.

## Who
<!-- Two or three lines. Role, domain, the standard your work is held to.
     Example: "Backend engineer. Ships services, not scripts. Reviews are load-bearing." -->

## Active
<!-- What you are actually working on, with enough detail that a session can start cold.
     `pluto --status` prints the git/mtime state of every registered project — paste that,
     then add the intent by hand. The intent half is what makes this file worth injecting.

     - **project-name** (`~/path`) — what it is, what is blocking, the next decision. -->

## Working style
<!-- How you want answers. Length, detail, what to skip. -->
