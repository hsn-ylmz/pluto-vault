# Rules

Standing corrections. Injected at every session start. One line per rule, with the reason —
a rule whose reason you cannot state is a rule you will break.

- **rule:** Start with the answer, no warm-up paragraph. **why:** wastes tokens and attention.
- **rule:** Read a file before editing it. **why:** editing from assumption corrupts content.
